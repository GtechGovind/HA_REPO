# ==============================================================================
# PostgreSQL Base Configuration Role
# ==============================================================================
# Description: Installs and prepares PostgreSQL on all database cluster nodes.
#              - Adds the official PostgreSQL repository and GPG key.
#              - Installs core packages, repmgr for failover, and utilities.
#              - Configures fundamental directories and system-level settings.
#              - Initializes the database on the primary node.
# ==============================================================================

- name: Add PostgreSQL repository key (using gpg)
  shell: curl -fsSL https://www.postgresql.org/media/keys/ACCC4CF8.asc | gpg --dearmor -o /usr/share/keyrings/postgresql.gpg
  args:
    creates: /usr/share/keyrings/postgresql.gpg

- name: Add PostgreSQL official repository
  apt_repository:
    repo: "deb [signed-by=/usr/share/keyrings/postgresql.gpg] http://apt.postgresql.org/pub/repos/apt {{ ansible_distribution_release }}-pgdg main"
    state: present

- name: Install PostgreSQL packages
  apt:
    name:
      - postgresql-{{ postgresql.version }}
      - postgresql-contrib-{{ postgresql.version }}
      - postgresql-{{ postgresql.version }}-repmgr
      - pgbackrest
      - postgresql-{{ postgresql.version }}-wal2json
      - python3-psycopg2
    state: present

- name: Create PostgreSQL basic directories
  file:
    path: "{{ item }}"
    state: directory
    owner: postgres
    group: postgres
    mode: '0750'
  loop:
    - /var/log/postgresql

- name: Stop PostgreSQL service completely
  systemd:
    name: postgresql
    state: stopped
  ignore_errors: yes
  when: postgresql_role == 'primary'

- name: Check if default PostgreSQL cluster exists
  shell: |
    pg_lsclusters --no-header | grep "{{ postgresql.version }} .*main" || true
  register: pg_clusters_check
  changed_when: false
  when: (postgresql_role == 'primary') or infra_reinstall

- name: Force stop PostgreSQL for reinstallation
  systemd:
    name: postgresql
    state: stopped
  when: infra_reinstall
  ignore_errors: yes

- name: Debug PostgreSQL cluster check
  debug:
    msg: 
      - "PostgreSQL cluster check for version {{ postgresql.version }} main"
      - "Return code: {{ pg_clusters_check.rc | default('N/A') }}"
      - "Stdout: {{ pg_clusters_check.stdout | default('N/A') }}"
      - "Stderr: {{ pg_clusters_check.stderr | default('N/A') }}"
      - "Infra Reinstall Flag: {{ infra_reinstall }}"
  when:
    - (postgresql_role == 'primary') or infra_reinstall
    - pg_clusters_check is defined

- name: Drop default PostgreSQL cluster to avoid conflicts
  command: pg_dropcluster {{ postgresql.version }} main --stop
  when:
    - ((postgresql_role == 'primary') or infra_reinstall)
    - pg_clusters_check is defined
    - '"main" in pg_clusters_check.stdout'
  ignore_errors: yes

- name: Remove PostgreSQL cluster configuration files
  file:
    path: "{{ item }}"
    state: absent
  loop:
    - "/etc/postgresql/{{ postgresql.version }}/main"
    - "/var/lib/postgresql/{{ postgresql.version }}/main"
    - "/var/log/postgresql/postgresql-{{ postgresql.version }}-main.log"
  when:
    - ((postgresql_role == 'primary') or infra_reinstall)
    - (pg_clusters_check is defined and "main" in pg_clusters_check.stdout) or infra_reinstall

- name: Remove stale PostgreSQL data directory if it exists
  file:
    path: "{{ item }}"
    state: absent
  loop:
    - "{{ postgresql.data_dir }}"
    - "{{ postgresql.wal_dir }}"
  when:
    - ((postgresql_role == 'primary') or infra_reinstall)
    - (pg_clusters_check is defined and "main" in pg_clusters_check.stdout) or infra_reinstall or infra_cleanup_data

- name: Initialize PostgreSQL data directory
  shell: |
    pg_createcluster {{ postgresql.version }} main \
    --start-conf=auto \
    -d {{ postgresql.data_dir }} \
    -- --waldir={{ postgresql.wal_dir }}
  args:
    creates: "{{ postgresql.data_dir }}/PG_VERSION"
  when: postgresql_role == 'primary'
  register: init_result
  failed_when:
    - init_result.rc != 0
    - '"already exists" not in init_result.stderr'
    - '"already exists" not in init_result.stdout'

- name: Create PostgreSQL configuration directory
  file:
    path: "{{ item }}"
    state: directory
    owner: postgres
    group: postgres
    mode: '0755'
  loop:
    - "/etc/postgresql/{{ postgresql.version }}/main"
    - "/etc/postgresql/{{ postgresql.version }}/main/conf.d"

- name: Debug PostgreSQL data directory initialization
  debug:
    var: init_result
  when:
    - postgresql_role == 'primary'
    - init_result is defined

- name: Configure PostgreSQL
  template:
    src: postgresql.conf.j2
    dest: /etc/postgresql/{{ postgresql.version }}/main/postgresql.conf
    owner: postgres
    group: postgres
    mode: '0644'
  notify: restart postgresql

- name: Configure pg_hba.conf
  template:
    src: pg_hba.conf.j2
    dest: /etc/postgresql/{{ postgresql.version }}/main/pg_hba.conf
    owner: postgres
    group: postgres
    mode: '0640'
  notify: restart postgresql

- name: Flush handlers to ensure configuration is applied
  meta: flush_handlers

- name: Ensure PostgreSQL service is started and enabled
  systemd:
    name: postgresql
    enabled: yes
    state: started
  when: postgresql_role == 'primary'

- name: Wait for PostgreSQL to be ready
  wait_for:
    port: "{{ postgresql.port }}"
    host: 127.0.0.1
    timeout: 90
  when: postgresql_role == 'primary'
  register: wait_result
  ignore_errors: yes

- name: Diagnostic - Check PostgreSQL service status
  systemd:
    name: postgresql
  register: pg_service_status
  when: 
    - postgresql_role == 'primary'
    - wait_result is failed

- name: Diagnostic - Debug PostgreSQL status
  debug:
    var: pg_service_status
  when: 
    - postgresql_role == 'primary'
    - wait_result is failed

- name: Diagnostic - Check PostgreSQL logs
  shell: |
    tail -n 50 /var/log/postgresql/postgresql-{{ postgresql.version }}-main.log || \
    journalctl -u postgresql --no-pager -n 50
  register: pg_logs_diag
  when: 
    - postgresql_role == 'primary'
    - wait_result is failed
  ignore_errors: yes

- name: Diagnostic - Debug PostgreSQL logs
  debug:
    var: pg_logs_diag.stdout_lines
  when: 
    - postgresql_role == 'primary'
    - wait_result is failed

- name: Fail if PostgreSQL was not ready
  fail:
    msg: "PostgreSQL failed to become ready within timeout on localhost:{{ postgresql.port }}. Check the diagnostic logs above."
  when: 
    - postgresql_role == 'primary'
    - wait_result is failed

- name: Debug PostgreSQL readiness
  debug:
    msg: "PostgreSQL is ready on localhost:{{ postgresql.port }}"
  when: 
    - postgresql_role == 'primary'
    - wait_result is succeeded

- name: Create replication user
  become_user: postgres
  postgresql_user:
    name: "{{ postgresql.replication_user }}"
    password: "{{ vault_replication_password }}"
    role_attr_flags: REPLICATION,LOGIN
    state: present
  when: postgresql_role == 'primary'