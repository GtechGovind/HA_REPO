# ==============================================================================
# Keepalived High Availability Role
# ==============================================================================
# Description: Configures VRRP (Virtual Router Redundancy Protocol) for
#              high availability of application and database entry points.
#              - Manages Virtual IP (VIP) addresses.
#              - Performs failover between nodes based on priority and health.
# ==============================================================================

- name: Install keepalived
  apt:
    name: keepalived
    state: present

- name: Force cleanup keepalived configuration for reinstallation
  file:
    path: "{{ item }}"
    state: absent
  loop:
    - /etc/keepalived/keepalived.conf
    - /etc/keepalived/keepalived-app.conf
    - /etc/keepalived/keepalived-db.conf
    - /etc/keepalived/health-check.sh
    - /etc/keepalived/notify.sh
  when: infra_reinstall

- name: Create keepalived script user
  user:
    name: keepalived_script
    system: yes
    shell: /sbin/nologin

- name: Configure keepalived configuration
  template:
    src: keepalived.conf.j2
    dest: /etc/keepalived/keepalived.conf
  notify: restart keepalived

- name: Create health check script
  template:
    src: health-check.sh.j2
    dest: /etc/keepalived/health-check.sh
    mode: '0755'
  notify: restart keepalived

- name: Ensure sudoers configuration for postgres health check
  copy:
    dest: /etc/sudoers.d/keepalived_postgres
    content: "keepalived_script ALL=(postgres) NOPASSWD: /usr/bin/psql\n"
    mode: '0440'
    validate: /usr/sbin/visudo -cf %s
  when: inventory_hostname in groups.get('db_nodes', [])

- name: Create notify script
  copy:
    src: ../../../scripts/failover-notify.sh
    dest: /etc/keepalived/notify.sh
    mode: '0755'
  notify: restart keepalived

- name: Ensure Keepalived log file exists with proper permissions
  file:
    path: /var/log/keepalived.log
    state: touch
    owner: keepalived_script
    group: root
    mode: '0644'
  changed_when: false

- name: Configure Keepalived syslog options
  lineinfile:
    path: /etc/default/keepalived
    regexp: '^KEEPALIVED_OPTIONS='
    line: 'KEEPALIVED_OPTIONS="-D -S 6"'
  notify: restart keepalived

- name: Enable and start keepalived
  systemd:
    name: keepalived
    enabled: yes
    state: started
  register: keepalived_service

- name: Debug keepalived service status
  debug:
    msg: "Keepalived service is running: {{ keepalived_service.changed or 'running' in (keepalived_service.status.ActiveState | default('')) }}"
  when: keepalived_service is defined and keepalived_service.status is defined

- name: Verify Keepalived VIP assignment
  shell: "ip addr show | grep -E '{{ network.app_vip }}|{{ network.db_vip }}' || true"
  register: vip_check
  changed_when: false

- name: Get Keepalived logs if VIP not assigned
  command: tail -n 50 /var/log/keepalived.log
  register: keepalived_logs
  changed_when: false
  when: vip_check.stdout == ""

- name: Show VIP assignment status
  debug:
    msg: 
      - "Assigned VIPs on {{ inventory_hostname }}: {{ vip_check.stdout_lines }}"
      - "Keepalived logs (last 50 lines) if VIP missing: {{ keepalived_logs.stdout_lines | default('VIP assigned or logs unavailable') }}"